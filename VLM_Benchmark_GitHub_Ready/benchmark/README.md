# Benchmark Scripts

- `prompt_policy.py`: canonical prompt policy for all 8 benchmark categories.
- `model_runners/`: model-specific benchmark runners.
- `run_scripts/`: shell launch scripts and batch runners.
- `build_final_annotations.py`: builds merged final annotation files.
